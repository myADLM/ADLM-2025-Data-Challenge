# --- Web UI ---
streamlit==1.36.0

# --- PDF loading / parsing ---
pypdf==4.2.0

# --- Retrieval (sparse + dense-friendly infra) ---
rank-bm25==0.2.2
faiss-cpu==1.8.0
numpy==1.26.4
rapidfuzz==3.9.3
tqdm==4.66.4

# --- LLM client (Groq) ---
groq==0.9.0
httpx==0.27.2
python-dotenv==1.0.1
tenacity==8.2.3

# --- Optional OCR pre-processing (only if you use ocrmypdf) ---
# External deps still required separately: Ghostscript + Tesseract + qpdf
ocrmypdf==16.3.0
pikepdf==9.0.0
Pillow==10.4.0
